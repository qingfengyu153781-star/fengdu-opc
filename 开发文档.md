# 枫独 · OPC 经营助手 — 开发文档

> 版本：v2.0（GOAI 初赛）｜最后更新：2026-08-07
> 所属：GOAI 世界人工智能开源大赛 · 无界应用｜BoundlessAgents · AI+金融 赛道
> 定位：面向**一人公司（OPC = One Person Company）**与零基础创业者的经营辅助 Agent，让没有财务/法务背景的普通人也能把公司开明白。

本文档面向**开发者与评委**：说明产品定位、技术架构、代码模块职责、部署方式与扩展指南。用户视角请见 `使用手册.md`，一页纸评审请见 `FOR_JUDGES_PROJECT_SUMMARY.md`。

---

## 一、产品与技术一句话

**产品**：用户一句话描述经营情况 → 系统匹配可申请政策 → 预审材料清单 → 诊断经营健康 → 提示资格风险。核心是「材料预审 + 经营诊断」闭环。

**技术**：**联网搜索匹配（核心）+ 规则判定资格 + LLM（语义理解）** 的混合架构，不是纯聊天机器人。

- **核心 = 联网搜索当地政策**（多源爬虫，可配 Bing API/SearXNG 更稳）→ 规则判定资格 → 材料清单 → 风险提示。
- **零预置搜索哲学**：政策/合规知识**不写死进代码**——换地区、换政策，靠模型实时搜索发现（详见第三节）。
- **LLM 负责理解**：语义抽取任意行业/地区、生成精准搜索词、合规问答、政策导入结构化。
- **断网保底**：联网搜索不可用（断网/受限）时降级本地通用库 + 规则引擎 → 不白屏、不造假（保命兜底，非核心卖点）。

---

## 二、技术架构总览

```
用户一句话经营描述
  → [business_profile 追问状态机]  缺哪些必填字段 → 逐项追问（主动询问，不依赖一次说清）
  → [rule_engine 政策匹配]          三态判定（match/no/unknown）→ 材料清单 → 缺项/格式检测
  → [state_model 状态模型]          6 维状态向量 → 三指数（健康 H / 政策机会 P / 风险 R）
  → [policy_searcher 实时搜索]      未收录地区实时搜索当地政策（OPC 热点词三档）
  → [app.py 驾驶舱 UI]             实时可视化 + 来源引用 + 风险提示
```

| 层 | 模块 | 职责 | 确定性 |
|----|------|------|:--:|
| 输入理解 | `utils/business_profile.py` | 18 字段经营信息模型 + 追问状态机 + 句法模式抽取（零值枚举） | ✅ 规则 |
| 政策匹配 | `utils/rule_engine.py` | 资格条件逐条判定、材料清单、风险评估 | ✅ 规则 |
| 经营诊断 | `utils/state_model.py` | 6 维状态向量 + 三指数（透明可解释） | ✅ 规则 |
| 实时搜索 | `utils/policy_searcher.py` | 未收录地区实时搜索当地政策（多源 + OPC 热点词 + 官方优先） | ✅ 规则 |
| LLM（语义理解） | `utils/api_client.py` | ModelScope 免费推理 API，多模型 fallback 链；负责行业/地区理解、搜索词生成、合规问答 | ⚠️ 有 key 才用 |
| 提示词 | `prompts/agent_prompts.py` | 多组 prompt 模板（人格/追问/抽取/解释/问答/导入） | ⚠️ 有 key 才用 |
| 政策数据 | `policies/` | 按地区分库（national 通用 + 温州真实 + 杭州实时搜索 + general 兜底 + 模板） | ✅ 数据 |
| UI 入口 | `app.py` | Gradio 6.x 枫叶主题，5 个 Tab + 驾驶舱 | — |

---

## 三、核心设计：零预置搜索哲学

**政策/合规知识全部靠模型实时搜索发现，不预先写进代码或政策文件。** 这是本作品与"预置政策库"类工具最本质的差异。

- **不预置城市/行业/政策枚举**：用户说什么地区就搜什么，模型理解任意行业。
- **搜索词三档**（`search_opc_policies`，用户完全可控）：
  1. **用户输入指定关键词** → 想搜什么就搜什么（优先级最高）
  2. **描述需求 → AI 提炼关键词**（`generate_keyword_from_desc`）→ 模型理解用户表达后提炼精准搜索词
  3. **自动 · OPC 场景热点词兜底** → `{地区} OPC 一人公司 创业补贴 工位注册 Token券 算力券`
- **实测效果**：主办城市杭州的新政（杭州市监局《支持一人公司 OPC 创新创业若干举措》）**靠热点词让模型实时搜到 gov.cn 官方原文**，全程未预置一条杭州政策。
- **一键入库**：搜到的政策可手动确认入库（热加载免重启），标「导入数据·待核对」，人工核对后才计入政策机会指数（零造假）。
- **断网降级**：实时搜索不可用时回退本地通用库，不白屏。

---

## 四、五大功能模块（Tab）

| Tab | 功能 | 输入 | 输出 |
|-----|------|------|------|
| ① 补贴材料预审 | 一句话描述经营情况，AI 主动追问补齐画像 | 经营描述 | 匹配政策 + 材料清单（已备/缺失/格式）+ 资格风险 |
| ② 经营健康诊断 | 填经营数据，生成三指数驾驶舱 | 经营数据 | 6 维向量 + 健康/政策机会/风险三指数 + 行动建议 |
| ③ 经营合规问答 | 问税务/合同/开票/社保问题 | 合规问题 | 带来源解答（可实时联网）；可让 AI 推荐该问的问题 |
| ④ 政策导入 | 粘贴任意地区真实政策原文 | 政策原文 | AI 结构化入库（热加载即用） |
| ⑤ 政策动态 · 地区对比 | 实时搜索任意地区政策 + 一键入库 + 两地区对比 | 地区 / 关键词 | 带官方来源的政策结果 + 结构化明细；地区对比表 |

---

## 五、代码模块说明

### `utils/business_profile.py` — 经营画像与追问
- 18 字段经营信息模型（地区/注册类型/行业/营收/社保/学历/毕业年份/团队规模/现金流等）
- **追问状态机**：根据已收集字段 + 政策匹配需要，逐项向用户追问缺失必填项（不依赖一次说清）
- **句法模式抽取**：零值枚举——用户说什么就识别什么，不依赖预置城市/行业表

### `utils/rule_engine.py` — 政策匹配与材料预审
- 资格条件逐条判定，输出三态：`match`（匹配）/ `no`（不满足）/ `unknown`（缺信息）
- 材料清单：已备 / 缺失 / 格式问题，逐项带 `source`（来源机构"去哪办"提示）
- 风险判定：社保缺口 / 客户集中 / 现金流缓冲等

### `utils/state_model.py` — 经营状态模型
- 6 维经营状态向量：个人依赖度 / 收入来源集中度 / 订单生命周期 / 客户集中风险 / 现金流缓冲 / 经营连续性
- 三指数：
  - **经营健康指数 H** = 5 维等权 20% 平均（收入稳定性/现金流安全/成本控制/政策匹配/经营周期），每维带打分理由，非黑盒
  - **政策机会指数 P** = 地区差异化可申请政策数（国家级人人都有，不计入机会）
  - **风险指数 R** = 规则综合判定（低/中/高）

### `utils/policy_searcher.py` — 实时搜索
- 多源爬虫搜索（Bing 国际 → DuckDuckGo → 百度），官方来源（.gov.cn）优先排序
- 官方原文内容提取：金额 / 资格 / 材料明细
- `search_opc_policies`：三档搜索词（用户 > LLM > OPC 热点词）
- `generate_keyword_from_desc`：描述需求 → LLM 提炼关键词

### `utils/api_client.py` — LLM 接入
- ModelScope 免费推理 API，多模型 fallback 链（失败自动切换）
- 未配 key 时全部功能走规则/句法兜底，不白屏

### `policies/` — 政策库
- `national.py`：国家级通用政策（人人适用）
- `wenzhou.py`：温州真实政策库（按官方文件收录，带 source）
- `hangzhou.py`：**不预置**——切换杭州自动触发实时搜索（零预置哲学）
- `general.py`：断网兜底通用库
- `region_template.py`：手工接入新地区的模板
- `__init__.py`：地区注册表 + 热加载（导入新政策免重启）

### `app.py` — Gradio UI
- 枫叶暖色主题，5 Tab 布局，顶部地区选择器（输入城市即切换）
- 驾驶舱实时可视化三指数 + 材料清单 + 来源引用
- 深色模式 CSS 自适应

---

## 六、本地部署

**前置**：Python 3.9+

```bash
# 1. 进入项目目录
cd demo

# 2. 安装依赖
pip install -r requirements.txt

# 3. 启动（Windows 可双击「启动.bat」）
python app.py

# 4. 浏览器打开
# http://localhost:7860
```

**可选：配置大模型增强**（语义理解 / 精准搜索词 / 合规问答深度增强）
- 设置环境变量 `MODELSCOPE_API_KEY`（从 ModelScope 官网获取）
- 不配置也可完整体验：句法 + 规则 + 联网搜索兜底

---

## 七、创空间在线部署

1. 在 ModelScope 创空间创建应用
2. 上传本项目代码（`.gitignore` 已排除本地文件）
3. 在「设置 / Secrets」配置环境变量 `MODELSCOPE_API_KEY`
4. 平台自动安装依赖并启动（运行命令 `python app.py`）

---

## 八、扩展指南：接入新地区政策

**方式 A：实时搜索（推荐，零预置）**
- 直接在顶部输入地区名（如"重庆"）→ 自动实时搜索当地政策
- 在「⑤ 政策动态」搜索 → 结果可一键入库

**方式 B：手工模板入库**
- 复制 `policies/region_template.py` 为新文件，填入地区名与政策条目（带 `source`/`source_url`）
- 在 `policies/__init__.py` 注册地区 → 热加载生效

**方式 C：政策导入（AI 结构化）**
- 「④ 政策导入」粘贴官方政策原文 → AI 自动结构化入库

> 合规要求：入库政策必须来自真实公开文件，带来源引用；未核验的政策标「待核对」。

---

## 九、合规与安全边界

| 边界 | 落实 |
|------|------|
| 辅助不替代 | 所有输出标注"辅助参考，不替代政府机构/金融机构/律师最终判断" |
| 不给确定结论 | 只给材料清单/预审表，不打包票"你能申领 X 元" |
| 零造假 | 政策只收录真实原文（带 source）；未核实政策标「待核对」、不计入正式匹配 |
| 涉法红线拦截 | 骗补/避税/虚报等请求立即拒绝，不协助 |
| 数据合规 | 政策数据来自公开文件；不收集真实个人信息；符合《个人信息保护法》 |
| 数据隔离 | 自助导入的政策打「待核对」标签，绝不计入正式"政策机会"指数 |

---

*开发文档 v2.0 · 2026-08-07 · 配合 `README.md`（仓库入口）、`使用手册.md`（用户操作）、`FOR_JUDGES_PROJECT_SUMMARY.md`（一页纸评审）阅读*
