# 🚀 部署命令（直接复制版）

> **目标**：把 `demo/` 推送到魔搭创空间 `qingfengyuqing/fengdu`
> **方法**：在终端（cmd / PowerShell / Git Bash 都行）逐条粘贴执行
> **防呆**：第 4 步会打印远程地址，**必须看到 `fengdu`**，不是 `feng`

---

## 打开终端 → 进入 demo 目录

> ⚠️ **cmd 跨盘符必须加 `/d`**。执行后提示符应变成 `D:\ai视频\output\GOAI比赛\demo>`，否则说明没切换成功。

**cmd（黑窗口）：**

```bash
cd /d "D:\ai视频\output\GOAI比赛\demo"
```

**PowerShell：**

```powershell
cd "D:\ai视频\output\GOAI比赛\demo"
```

**Git Bash：**

```bash
cd /d/ai视频/output/GOAI比赛/demo
```

> 验证：执行后命令行前缀应变成 `D:\ai视频\output\GOAI比赛\demo>`。如果还是 `C:\...`，重新执行上面的 cmd 版（带 `/d`）。

---

## 第 1 步：设置身份（只设一次）

```bash
git config user.name "qingfengyuqing"
```

```bash
git config user.email "you@modelscope.cn"
```

---

## 第 2 步：添加并提交所有文件

```bash
git add -A
```

```bash
git commit -m "枫独 OPC 经营助手 v1.0（GOAI 初赛 Demo）"
```

> 如果提示 `nothing to commit` 或已提交过，直接跳过到第 3 步。

---

## 第 3 步：添加远程仓库（只添加一次）

```bash
git remote add modelscope https://oauth2:ms-xxx@www.modelscope.cn/studios/qingfengyuqing/fengdu.git
```

> ⚠️ **把 `ms-xxx` 换成真实地址**：回到魔搭创空间页面，复制它给出的 **clone / git 地址**（形如 `https://oauth2:ms-真实token@www.modelscope.cn/studios/qingfengyuqing/fengdu.git`），整段替换到上面命令里再执行。
> 如果报 `remote modelscope already exists` → 说明已添加过，直接到第 4 步。

---

## 第 4 步：★ 防呆检查（必须看到 fengdu）

```bash
git remote -v
```

> 输出必须长这样（末尾 **fengdu**）：
> ```
> modelscope  https://oauth2:ms-xxx@www.modelscope.cn/studios/qingfengyuqing/fengdu.git (fetch)
> modelscope  https://oauth2:ms-xxx@www.modelscope.cn/studios/qingfengyuqing/fengdu.git (push)
> ```
> ⛔ 如果出现 `feng` 而不是 `fengdu` → **停止**，发我，别继续。

---

## 第 5 步：合并远程默认文件 + 推送

```bash
git fetch modelscope
```

```bash
git pull modelscope master --allow-unrelated-histories --no-edit
```

```bash
git push -u modelscope master
```

---

## ✅ 推完后的验证

| 动作 | 期望 |
|------|------|
| 刷新创空间页面 | 文件列表出现 25 个文件（app.py / assets / policies / utils / ...）+ `.gitignore` |
| **不该出现** | `.backup/`、`__pycache__/`、`*.log`、`bash.exe.stackdump` |
| 创空间「部署配置」页 | 填环境变量 `MODELSCOPE_API_KEY` → 部署 |
| 部署状态 | Validation passed → 在线 URL 可访问 |

---

## ⚠️ 安全提醒

- 创空间地址里的 `ms-xxx` 是访问 token，**只存本机终端 / git 配置**，不要写进任何代码 / README / 上传文件
- 执行时从创空间页面复制的地址，只用于粘贴到终端，不要保存到 demo 目录内任何文件
